/**
 * Green chip shown on catalog cards and table rows when the listing is
 * already in the user's apps.
 */
export default function InYourAppsBadge() {
  return (
    <span className="inline-block px-2 py-0.5 rounded-sm bg-success/10 text-success text-xs font-medium flex-shrink-0">
      In your apps
    </span>
  );
}
